{
  "name": "mybot",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push origin master --no-verify && git push --tags origin master --no-verify"
  },
  "author": "drillep",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.502.0",
    "discord.js": "^11.5.1",
    "dotenv": "^8.0.0",
    "node-fetch": "^2.6.1"
  }
}
